# inClassExercise-Quiz 6

Create the Add CourseForm Functionality
1. Create the model function, addCourse() in the **models/courseDB.js**
2. Create the form view template with a BootStrap Form to add a course **(AddCourse.ejs)** in the **views** directory
3. Create a route and controller to display the form on **"localhost:3000/courses/add"**
4. Create a route and controller to process the form submission and add it to the courses table on **HTTP POST, :localhost:3000/courses/add"**
4. Redirect to the form page.